from setuptools import setup

setup(name='aiinpy',
      version='0.0.12',
      description='An artificial intelligence library',
      license='MIT',
      packages=['aiinpy'],
      install_requires=['numpy']
      )